這是一個關於 Backtracking (回溯法) 的階段性總結。回顧這 10 道題目，你的成長非常顯著，從一開始對遞迴的模糊，到後來能夠掌握核心模板，以下是我觀察到的關鍵轉折點：

### 1. **決策樹的具象化 (Visualizing the Decision Tree)**
- **狀況**：在剛開始做 **Subsets (78)** 和 **Permutations (46)** 時，你對於「For 迴圈」在遞迴裡的角色比較困惑。不知道什麼時候該從 `i = start` 開始，什麼時候該從 `i = 0` 開始。
- **能力缺口**：**「狀態空間樹 (State-Space Tree) 的構建」**。
  - 你逐漸理解了：
    - **組合 (Combination/Subset)** 問題：不走回頭路，所以傳入 `start_index`，下一層從 `i + 1` 開始。
    - **排列 (Permutation)** 問題：每次都重新洗牌，所以每次從 `0` 開始，但需要一個 `used` 陣列或 `Set` 來標記誰用過了。

### 2. **去重與剪枝的藝術 (Pruning & Deduplication)**
- **狀況**：在 **Subsets II (90)** 和 **Combination Sum II (40)** 這兩題，你遇到了「相同元素導致重複結果」的難題。一開始很難理解為什麼 `i > start && nums[i] == nums[i-1]` 這行神奇的代碼可以解決問題。
- **能力缺口**：**「層級 vs 深度 (Level vs Depth) 的去重」**。
  - 這是一個高階觀念。你學會了我們允許「深度」上有重複（例如 `[2, 2]` 是合法的），但不允許「同一層」有重複選擇（例如選了第一個 2，就不能同層再選第二個 2）。
  - **關鍵操作**：Input Array 必須先 **Sort (排序)**！這是所有去重策略的前提。

### 3. **引用與快照 (References vs Snapshots)**
- **狀況**：在 JavaScript 中，這是最常見的陷阱。你可能遇過 `res.push(path)` 結果最後印出來全是空陣列 `{}` 或最後狀態的情況。
- **能力缺口**：**「記憶體管理與副作用」**。
  - 因為 Backtracking 的本質是不斷修改同一個 `path` 變數 (Push -> Recurse -> Pop)。
  - 若要保存當下的結果，必須進行 **Deep Copy (深拷貝)** 或 **Shallow Copy**，例如 `res.push([...path])`。這是「記錄當下瞬間」的重要技巧。

### 🎯 總結與建議

Backtracking 是一個「暴力法」的優化版本，核心在於**模板 (Template)** 的熟練度。

接下來面對更難的題目（如 N-Queens 或 Word Search）：

1. **先判斷「要不要排序」**：只要題目提到「不能有重複組合」且輸入陣列可能有重複數字，**起手式就是先 Sort**。
2. **條件反射模板**：
   ```js
   function backtrack(start, path) {
       // 1. 終止條件 (Base Case)
       if (condition) { res.push([...path]); return; }
       
       // 2. 決策迴圈
       for (let i = start; i < nums.length; i++) {
           // 3. 剪枝 (Pruning)
           if (i > start && nums[i] == nums[i-1]) continue; 
           
           // 4. 做選擇 (Choose) & 遞迴 (Explore)
           path.push(nums[i]);
           backtrack(i + 1, path);
           
           // 5. 撤銷選擇 (Unchoose / Backtrack)
           path.pop();
       }
   }
   ```
3. **時間複雜度敏感度**：Backtracking 通常是指數級別 O(2^N) 或階乘級別 O(N!)。看到數據範圍 N <= 20，通常就是暗示要用 Backtracking。

恭喜你攻克了這 10 道 Backtracking 經典題！這個類別是面試中的常客，保持這個手感！
