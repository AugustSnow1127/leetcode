# 1046. Last Stone Weight

## 問題描述

給定一堆石頭，每顆石頭都有一個正整數重量。

每一回合，我們選出兩顆 **最重** 的石頭，假設重量分別為 `x` 和 `y` (x <= y)。將它們兩兩撞擊：

1. 如果 `x == y`，兩顆石頭都會完全粉碎（消失）。
2. 如果 `x != y`，那麼重量為 `x` 的石頭消失，重量為 `y` 的石頭變成 `y - x`。

最後如果不剩石頭，返回 0；如果剩下一顆，返回它的重量。

## 解題思路

（待補充）
