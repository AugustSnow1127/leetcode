# Binary Search
**LeetCode**: #704 | **Difficulty**: Easy |
**Category**: Binary Search

## 🔍 Problem Statement
給定一個已排序的整數陣列 `nums`（升序）和一個目標值 `target`，返回 `target` 的索引。如果不存在，返回 -1。

必須寫出時間複雜度為 O(log n) 的演算法。

**Example 1:**
```
Input: nums = [-1,0,3,5,9,12], target = 9
Output: 4
Explanation: nums[4] = 9
```

**Example 2:**
```
Input: nums = [-1,0,3,5,9,12], target = 2
Output: -1
Explanation: 2 不存在於 nums 中
```

## 💡 解題思路

### 關鍵洞察
- **核心觀察**：陣列已排序，每次比較可以排除一半的搜尋空間
- **問題特徵**：在有序空間中找特定值

### 演算法選擇
- **為什麼選 Binary Search？**
  - 線性搜尋：O(n) 太慢
  - **Binary Search**：每次將範圍減半，O(log n)

- **適用場景**
  - 當資料已排序時
  - 當需要快速查找時
  - 複雜度分析：時間 O(log n)，空間 O(1)

### 時機判斷
看到以下特徵時，考慮使用 Binary Search：
- 📊 關鍵字：「有序」、「排序」、「搜尋」、「O(log n)」
- 🎯 限制：必須是已排序的資料
- 💡 優化：從 O(n) 降為 O(log n)

## 🎯 實作細節

### 步驟分解
1. **初始化**：left = 0, right = len(nums) - 1
2. **二分查找**：while left <= right
3. **計算中點**：mid = (left + right) // 2
4. **比較並收縮**：
   - 如果 nums[mid] == target，返回 mid
   - 如果 nums[mid] < target，target 在右半，left = mid + 1
   - 如果 nums[mid] > target，target 在左半，right = mid - 1
5. **未找到**：返回 -1

### 關鍵程式碼片段
```python
def search(nums, target):
    left, right = 0, len(nums) - 1
    while left <= right:
        mid = (left + right) // 2
        if nums[mid] == target:
            return mid
        elif nums[mid] < target:
            left = mid + 1
        else:
            right = mid - 1
    return -1
```

**為什麼這樣寫？**
- `while left <= right`：確保所有可能都被檢查
- `mid = (left + right) // 2`：整數除法向下取整
- `left = mid + 1` 和 `right = mid - 1`：排除已檢查的 mid

### 易錯點
⚠️ **坑點1**：條件寫成 `while left < right` - 會漏掉邊界情況
⚠️ **坑點2**：收縮時沒有 +/- 1 - 會造成無限迴圈
⚠️ **坑點3**：整數溢出（Python 不會，其他語言要小心）

## 📊 複雜度分析

| 方法 | 時間複雜度 | 空間複雜度 | 優點 | 缺點 |
|------|------------|------------|------|------|
| 線性搜尋 | O(n) | O(1) | 簡單 | 太慢 |
| **Binary Search** | O(log n) | O(1) | **極快** | 需要有序資料 |

## 🤔 相關問題
- [Search a 2D Matrix] - 2D 版本的 Binary Search
- [Find Minimum in Rotated Sorted Array] - 變形的 Binary Search

## 📝 學習筆記
- **初學重點**：理解每次迭代將搜尋空間減半的概念
- **模式識別**：看到「有序 + 搜尋」就想到 Binary Search
- **技巧**：畫出 left, mid, right 的位置變化圖幫助理解

## 🔑 關鍵模式
**Binary Search 標準模板**
```python
left, right = 0, len(nums) - 1
while left <= right:
    mid = (left + right) // 2
    if nums[mid] == target:
        return mid
    elif nums[mid] < target:
        left = mid + 1
    else:
        right = mid - 1
return -1
```

## 🎯 追蹤你的進度
- [ ] 完成第一次解題
- [ ] 不看解答重新解題
- [ ] 嘗試遞迴版本
- [ ] 向他人解釋為什麼是 O(log n)

---
**題目連結**: [LeetCode 704](https://leetcode.com/problems/binary-search/)
